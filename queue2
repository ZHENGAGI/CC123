#include<bits/stdc++.h>
#include<queue>
using namespace std;
 
int main(){

	queue<int> q;
	q.push(10);
	q.push(20);
	q.push(30);
	
	queue <int> q1;
	q1.push(20);
	q1.push(30);
	q1.push(40);
    swap (q1,q);
	while(!q.empty()){
		cout<<q.front()<<"\t";
		q.pop();
	
	}
	return 0;
}
